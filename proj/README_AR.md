# كيفية تشغيل الشات بوت

## ⚡ الطريقة السريعة (Windows):
1. افتح ملف `START_SERVER.bat` بالنقر المزدوج
2. انتظر حتى يظهر "Running on http://localhost:5000"
3. افتح الموقع في المتصفح واذهب إلى صفحة Chat Bot

## 📝 الطريقة اليدوية:

### الخطوة 1: افتح Terminal/Command Prompt
- اضغط `Win + R` واكتب `cmd` ثم Enter
- أو ابحث عن "Command Prompt" في قائمة Start

### الخطوة 2: اذهب إلى مجلد proj:
```bash
cd C:\xampp\htdocs\SmartClinic\proj
```

### الخطوة 3: تأكد من تثبيت المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```
**ملاحظة:** قد يستغرق هذا بضع دقائق في المرة الأولى

### الخطوة 4: شغّل Flask Server:
```bash
python controller.py
```

### الخطوة 5: يجب أن ترى:
```
==================================================
SmartClinic Chatbot Server
==================================================
Server starting on: http://localhost:5000
Press Ctrl+C to stop the server
==================================================

 * Running on http://127.0.0.1:5000
```

## ⚠️ ملاحظات مهمة:
- ✅ **يجب أن يكون Flask Server شغال** قبل فتح صفحة `chatbot.php`
- ✅ Server يعمل على `http://localhost:5000`
- ✅ **لا تغلق Terminal** أثناء استخدام الشات بوت
- ✅ لإيقاف Server: اضغط `Ctrl+C` في Terminal

## 🔧 استكشاف الأخطاء:

### خطأ: "ModuleNotFoundError"
**الحل:** شغّل الأمر التالي:
```bash
pip install -r requirements.txt
```

### خطأ: "Port 5000 already in use"
**الحل:** 
- أغلق البرنامج الذي يستخدم المنفذ 5000
- أو غيّر المنفذ في `controller.py` (السطر الأخير)

### خطأ: "Python is not recognized"
**الحل:**
- تأكد من تثبيت Python 3.8 أو أحدث
- أضف Python إلى PATH أثناء التثبيت
- أعد تشغيل Terminal بعد تثبيت Python

### الشات بوت لا يعمل في الموقع
**الحل:**
1. تأكد من أن Flask Server شغال (راجع Terminal)
2. افتح `http://localhost:5000` في المتصفح - يجب أن ترى صفحة
3. تحقق من أن `chatbot_api.php` يشير إلى `http://localhost:5000`

## 📋 قائمة المكتبات المطلوبة:
- Flask (للسيرفر)
- flask-cors (للسماح بالاتصال من PHP)
- numpy, pandas, scikit-learn (للنماذج)
- catboost (للنموذج M1)
- openpyxl (لقراءة ملفات Excel)
- requests (للتواصل مع APIs)

## 🚀 بعد التشغيل:
1. اترك Terminal مفتوحاً
2. افتح الموقع في المتصفح
3. سجّل الدخول كـ Patient
4. اذهب إلى صفحة "Chat Bot"
5. اضغط "بدء المحادثة"

---

**تم إنشاء هذا الملف لمساعدتك في تشغيل الشات بوت بسهولة!** 🎉











